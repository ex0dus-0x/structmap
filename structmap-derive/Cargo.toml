[package]
name = "structmap-derive"
description = "Proc derive macro for structmap crate"

authors = ["ex0dus-0x <ex0dus@codemuch.tech>"]
license = "MIT"

version = "0.1.1"
edition = "2018"

[lib]
proc-macro = true

[dependencies]
quote = "1.0"
syn = { version = "1.0", features = ["full"] }
